import { makeStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles((theme) => ({

    container: {
        position: "absolute",
        right: "100px",
        top: "10%",
        height:"10%"
        
}

}))
